{"n": 1, "status": "open", "severity": "medium", "area": "sqlite", "file": "src/server/sqlite.ts", "summary": "Two processes on one SQLite file: no busy_timeout, deferred transactions, and caches that never hear each other", "detail": "From the v0.5.0 review (TODO #8, Reported). Nothing sets PRAGMA busy_timeout, and writes use db.transaction() (deferred; .immediate() is the fix), so the first write after another process's read lock fails SQLITE_BUSY at once and a valid write 500s. And the in-memory document cache is per process with no invalidation, so two server processes on one file serve divergent state. Either document one process per file as a hard rule (the skill's feature table now says 'one per file') or give SQLite a change channel like the Postgres listener. Repro: two `bun` processes calling registerDocs on the same file, one holding a read transaction while the other writes.", "note": "2026-09-24: carried from TODO.md #8; still open after round 2."}
{"n": 2, "status": "open", "severity": "medium", "area": "sqlite", "file": "src/server/sqlite.ts", "summary": "An included collection with no parent is filtered by the root's scope, and its rows fan out to every document of the prefix", "detail": "From the v0.5.0 review (TODO #9, Reported). loadCollection builds `WHERE <root scope columns> = ?` against the unparented collection's own table (with the default scope, `WHERE id = <docId>`: at most one row), and rowInScope returns true for any unparented collection, so a write to it reaches every open document of that prefix. In a tenant-scoped schema with a shared unparented collection that is a cross-document read. Needs a decision on what an unparented include means (load it all, as Postgres list-mode include does, or refuse it in defineDoc). No test covers the shape.", "note": "2026-09-24: carried from TODO.md #9; needs a decision."}
{"n": 3, "status": "open", "severity": "low", "area": "core", "file": "src/server/server.ts, .claude/skills/delta-doc/SKILL.md", "summary": "A whole-row replace means two things: the JSON file replaces the row, SQLite and Postgres merge the fields sent", "detail": "From the second review pass (TODO A3). `replace /coll/id` with a partial value drops the missing fields on the JSON-file backend (RFC 6902) and merges them on SQLite and Postgres. The skill's 'Backends side by side' table now says so ('send it whole'). Either registerDoc merges at depth 2 to match, or the SQL backends replace. Repro: scratchpad/delta-reviewer/parity.ts, case 'whole-row replace with partial value'.", "note": "2026-09-24: carried from TODO.md A3; documented, not unified."}
{"n": 4, "status": "open", "severity": "medium", "area": "core", "file": "src/core.ts, tests/core.test.ts", "summary": "applyOps is RFC 6901 now but not RFC 6902: add at an array index overwrites, and replace/remove of a missing member do not fail", "detail": "Round 2 made the pointer grammar strict (leading /, escaping, a segment a string unless its parent is an array, no holes) and batches atomic. Two RFC 6902 rules are still delta's own: `add /arr/1` assigns instead of inserting (pinned by tests/core.test.ts 'add at array index OVERWRITES', documented in core.ts), and `replace`/`remove` of a member that is not there sets it / does nothing instead of failing (so the JSON file acks `remove /messages/nope` where SQLite answers 404). eta keeps its own src/json-patch.ts because it relies on insert (examples/ticker watchlist undo). Making delta's applyOps full RFC 6902 would let eta drop its copy; it changes documented behaviour, so it is Pete's call. Note the client applies broadcasts with the same applyOps, so a strict remove must not break fan-out of a row a client never had.", "note": "2026-09-24: round-2 proposal; for Pete."}
{"n": 5, "status": "open", "severity": "medium", "area": "client", "file": "src/server/server.ts, src/client/client.ts", "summary": "The server handles a socket's messages concurrently, so a signed-in client that doesn't use onConnect still races its own authenticate", "detail": "Round 2 fixed the reconnect freeze on the client (connectWs(url, { onConnect }) runs before any doc (re)opens). Without the hook, two things remain: (1) createWs's message handler is async and not serialised per socket, so an `open` sent right after `authenticate` can be handled first and 401 (the 'variant B' of scratchpad/delta-reviewer/reconnect-auth.ts, re-run as scratchpad/round2-delta-fixer/reconnect-auth.ts); (2) a doc whose re-open got a 401 is only logged, never retried after a later authenticate. Per-socket serialisation would block slow handlers (recompute, registerMethod), so make it opt-in if it is done.", "note": "2026-09-24: the server half of D2; client half fixed in round 2."}
{"n": 6, "status": "open", "severity": "medium", "area": "sqlite", "file": "src/server/sqlite.ts", "summary": "SQLite has no auth: no gate, no owns, every socket may open every document of a prefix", "detail": "D9 from round 1. registerDocs takes no DeltaAuth, and open/delta never ask who is writing, so the SQLite tier cannot be multi-user safe without hand-written ws.on shims. The skill now says 'no auth' in the feature table and the SQLite quick start. A minimal fit with round 2's Postgres design: registerDocs(..., { auth, owns }) with the same default-deny as docTypeFromDef.", "note": "2026-09-24: round-2 proposal; documented, not built."}
{"n": 7, "status": "open", "severity": "low", "area": "sqlite", "file": "src/server/sqlite.ts", "summary": "SQLite documents have no list mode, and a root row can only be made over the wire by an implied document", "detail": "D9 / dogfood A3, A4, P5. `defineDoc('todos:', { root: 'todos' })` opened as `todos:` looks for the row id '' and answers 404 (the message now says why). The shape of a document therefore differs from Postgres ({todos} there, {lists, todos} here). implied: true covers creating the root on first write; seeding with SQL covers the rest. Postgres also needs a raw INSERT for a single-mode root row.", "note": "2026-09-24: round-2 proposal; the 404 names the rule now."}
{"n": 8, "status": "open", "severity": "low", "area": "sqlite", "file": "src/server/sqlite.ts", "summary": "SQLite opens and broadcasts carry valid_from and valid_to; Postgres strips them", "detail": "Seen while writing the SQLite quick start (scratchpad/round2-delta-fixer/sqlite-quickstart.ts): the open snapshot's rows and every echo include the temporal storage columns, which then sit in the client's doc.data. Postgres runs _delta_strip_temporal on both. inverseOf already strips them (withoutStorage). Stripping them on SQLite too would make the document shape the same on both SQL backends.", "note": "2026-09-24: found in round 2; not fixed."}
{"n": 9, "status": "open", "severity": "low", "area": "core", "file": "src/server/sqlite.ts, src/server/postgres/schema.ts, src/schema.ts", "summary": "Two validateOps: SQLite's (types, root fields) and Postgres's (a subset, which the listener never calls)", "detail": "CLAUDE.md used to say validateOps lived in src/schema.ts, shared; it does not. sqlite.ts has the full one; postgres/schema.ts exports a smaller one for callers, and the Postgres listener relies on delta_apply's checks instead (round 2 added unknown and required fields there). One shared validator is the DRY answer, but Postgres list-mode documents address rows on the root collection where SQLite treats /<root>/<x> as a root field, so it needs the document's mode.", "note": "2026-09-24: round-2 proposal."}
{"n": 10, "status": "open", "severity": "low", "area": "client", "file": "src/client/client.ts", "summary": "doc.data is typed as a writable Signal, so an optimistic doc.data.set() type-checks", "detail": "Round 1 (delta.md §4). The skill's first rule is 'never optimistically update', but Doc<T>.data is ReturnType<typeof signal<T|null>>, so .set() and .patch() compile. Exposing ReadonlySignal<T|null> would make the rule a type error. It is a type-level breaking change for anyone who calls set() today.", "note": "2026-09-24: round-2 proposal."}
{"n": 11, "status": "open", "severity": "low", "area": "cli", "file": "cli.ts", "summary": "The CLI is not agent-safe yet: no in-band token, watch never exits, delta answers only {ack}", "detail": "Round 1 (delta.md §4, §5). An auth-enabled server can't be reached (the token must not go in the URL, so it needs an in-band `call authenticate` first: --token / DELTA_TOKEN); `watch` never exits, so a Bash tool call hangs (--count N, --for 5s); `delta` returns only {ack}, not the ops as applied (--print-ops); and there is no offline `validate` or server `describe`. These come before an MCP wrapper.", "note": "2026-09-24: round-2 proposal."}
{"n": 12, "status": "open", "severity": "low", "area": "release", "file": ".github/workflows/ci.yml, .github/workflows/publish.yml, .claude/commands/publish.md", "summary": "CI installs the latest Bun unpinned, and /publish gates on bun run ci, which needs Docker", "detail": "ci.yml has `bun-version: latest` and publish.yml no version, so a Bun release can turn the gate red with no change here (round 1 saw exactly that on 1.4.2; the teardown was fixed upstream in 0.6.0). /publish runs `bun run ci` (Docker compose), which cannot run in the Claude Code web sandbox; CLAUDE.md gives the container gate instead. The release text is shared with railroad and eta.", "note": "2026-09-24: round-2 proposal; release tooling is Pete's."}
{"n": 13, "status": "open", "severity": "low", "area": "docs", "file": "examples/todos-vs-rls/delta.ts", "summary": "examples/todos-vs-rls writes with raw INSERT/UPDATE, which the skill forbids", "detail": "Round 1 D11. delta.ts:120-139 writes the table directly, so nothing NOTIFYs; the example is never live, and its shared `todos:me` name is the D1 shape (with auth it now needs owns). Rewrite it on docTypeFromDef with owns, or drop it.", "note": "2026-09-24: carried from round 1; examples are not in the package."}
{"n": 14, "status": "open", "severity": "low", "area": "kinds", "file": "src/server/kinds.ts", "summary": "A memory document's add /coll/- stores the key \"-\"; the JSON file and SQLite mint an id", "detail": "Round 2 made `add /<coll>/-` on a collection of rows mint an id on the JSON file (server.ts mintIds) and SQLite. registerMemory applies ops with applyOps directly, so `/people/-` on an object makes the key '-'. Memory documents are written by the server, so it bites less; mintIds could move to core for both.", "note": "2026-09-24: found in round 2; not fixed."}
{"n": 15, "status": "open", "severity": "low", "area": "tests", "file": "tests/setup.ts, tests/postgres-pointer.test.ts, package.json", "summary": "Share the test surface: @blueshed/delta/testing, and the pointer vectors as data eta can run", "detail": "Consumers can't import mockClient/sendAndAwait (tests/ is not in the package; the skill now says so). And the one pointer grammar is pinned by VECTORS in tests/postgres-pointer.test.ts, run through splitPath and _delta_split_path; as a JSON fixture (wire-vectors.json) eta's json-pointer.ts could run the same cases without a dependency (proposal-final.md §4). The team's larger idea is backend conformance stories (who does what, who sees it) run against JSON, SQLite and Postgres; tests/create-row.test.ts and tests/error-codes.test.ts are the first two.", "note": "2026-09-24: round-2 proposal."}
